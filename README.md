# CampusConnect University Chatbot - Role Access Fixed

This version uses app(9) with a compatible engine.py that includes decide_visual_response.

Run:

```bash
pip install -r requirements.txt
streamlit run app.py
```

Default engineer password: engineer123
